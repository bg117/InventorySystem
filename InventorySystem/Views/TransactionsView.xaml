<UserControl x:Class="InventorySystem.Views.TransactionsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:mah="http://metro.mahapps.com/winfx/xaml/controls"
             xmlns:local="clr-namespace:InventorySystem.Views"
             xmlns:viewmodels="clr-namespace:InventorySystem.ViewModels"
             xmlns:iconPacks="http://metro.mahapps.com/winfx/xaml/iconpacks"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.DataContext>
        <viewmodels:TransactionsViewModel/>
    </UserControl.DataContext>
    <StackPanel Orientation="Vertical">
        <StackPanel Orientation="Horizontal" Margin="0 0 0 8">
            <Button Margin="0 0 4 0">
                <StackPanel Orientation="Horizontal">
                    <iconPacks:PackIconMaterial Kind="Plus"/>
                    <TextBlock Margin="4 0">Add transaction</TextBlock>
                </StackPanel>
            </Button>
            <Button Command="{Binding RemoveSelectedCommand}">
                <StackPanel Orientation="Horizontal">
                    <iconPacks:PackIconMaterial Kind="TrashCan"/>
                    <TextBlock Margin="4 0">Remove selected</TextBlock>
                </StackPanel>
            </Button>
        </StackPanel>
        <StackPanel Orientation="Vertical">
            <StackPanel Orientation="Horizontal" Margin="0 0 0 4">
                <TextBox mah:TextBoxHelper.Watermark="Search by ID" Margin="0 0 4 0" Width="248"/>
                <Button>
                    <iconPacks:PackIconMaterial Kind="Magnify"/>
                </Button>
            </StackPanel>
            <StackPanel Orientation="Horizontal"  Margin="0 0 0 4">
                <TextBox mah:TextBoxHelper.Watermark="Filter by item name" Margin="0 0 4 0" Width="248"/>
                <Button>
                    <iconPacks:PackIconMaterial Kind="Magnify"/>
                </Button>
            </StackPanel>
        </StackPanel>
        <ListView x:Name="TransactionsListView" ItemsSource="{Binding Transactions}" SelectionMode="Extended" SelectionChanged="Selector_OnSelectionChanged">
            <ListView.View>
                <GridView>
                    <GridView.Columns>
                        <GridViewColumn Header="ID" DisplayMemberBinding="{Binding Id}"/>
                        <GridViewColumn Header="Transaction Date" DisplayMemberBinding="{Binding Date}"/>
                        <GridViewColumn Header="Item Name" DisplayMemberBinding="{Binding ItemName}"/>
                        <GridViewColumn Header="Qty" DisplayMemberBinding="{Binding Quantity}"/>
                        <GridViewColumn Header="Status" DisplayMemberBinding="{Binding Status}"/>
                        <GridViewColumn Header="Price" DisplayMemberBinding="{Binding TotalPrice}"/>
                    </GridView.Columns>
                </GridView>
            </ListView.View>
        </ListView>
    </StackPanel>
</UserControl>
